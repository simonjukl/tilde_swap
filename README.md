# tilde-swap

Swap the sides of R formula expressions (`~`).

Place your cursor on a line like `y ~ x` and run **R: Swap Formula Sides (~)** (`Ctrl+Alt+S` / `Cmd+Alt+S`) to get `x ~ y`.

Works on the current line or a selection spanning multiple lines.
