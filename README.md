# tilde-swap

Swap the sides of R formula expressions (`~`).

Place your cursor on a line like `y ~ x` and run **Tilde Swap: Swap Formula Sides (~)** from the Command Palette or press `Ctrl+Alt+S` / `Cmd+Alt+S` to get `x ~ y`.

Works on the current line or a selection spanning multiple lines.
